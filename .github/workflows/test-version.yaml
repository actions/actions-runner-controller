name: Test version


on:
  workflow_dispatch:
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Set version
        run: echo "VERSION=test" >> $GITHUB_ENV
      - name: test env
        run: echo $VERSION
